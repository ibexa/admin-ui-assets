import React from 'react';
import { BaseInputProps } from './BaseInput.types';
declare const BaseInput: ({ name, disabled, error, extraClasses, extraInputAttrs, id, required, size, title, type, value, }: BaseInputProps) => React.JSX.Element;
export default BaseInput;
