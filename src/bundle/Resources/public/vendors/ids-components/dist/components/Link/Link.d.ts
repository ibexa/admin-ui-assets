import React from 'react';
import { LinkProps } from './Link.types';
export declare const Link: ({ onClick, children, ariaLabel, disabled, href, target, rel, className, extraAria, title, variant, size, type, icon, iconUrl, }: LinkProps) => React.JSX.Element;
