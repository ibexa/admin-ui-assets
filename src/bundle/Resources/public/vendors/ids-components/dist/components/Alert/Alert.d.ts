import React from 'react';
import { AlertProps } from './Alert.types';
export declare const Alert: ({ actions, children, className, icon, iconPath, isDismissible, onDismiss, role, title, type, variant, }: AlertProps) => React.JSX.Element;
