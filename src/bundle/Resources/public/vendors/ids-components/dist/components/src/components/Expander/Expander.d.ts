import React from 'react';
import { ExpanderProps } from './Expander.types';
export declare const Expander: ({ onClick, type, collapseLabel, expandLabel, hasIcon, isExpanded }: ExpanderProps) => React.JSX.Element;
