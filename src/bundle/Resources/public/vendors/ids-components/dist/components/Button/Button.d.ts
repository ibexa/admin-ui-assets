import React from 'react';
import { ButtonProps } from './Button.types';
export declare const Button: ({ onClick, children, ariaLabel, disabled, extraAria, className, icon, isFocusable, size, title, type, }: ButtonProps) => React.JSX.Element;
