import React from 'react';
import { ButtonProps } from './Button.types';
export declare const Button: ({ onClick, children, ariaLabel, disabled, extraAria, className, icon, iconUrl, isFocusable, ref, size, title, type, iconPosition, ...nativeButtonProps }: ButtonProps) => React.JSX.Element;
