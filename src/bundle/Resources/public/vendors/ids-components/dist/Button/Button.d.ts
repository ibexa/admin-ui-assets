import React from 'react';
import { ButtonProps } from './Button.types';
declare const Button: ({ onClick, children, ariaLabel, disabled, extraAria, extraClasses, icon, size, title, type, }: ButtonProps) => React.JSX.Element;
export default Button;
