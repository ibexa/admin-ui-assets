import React from 'react';
import { ButtonProps } from './Button.types';
declare const Button: ({ type, size, ariaLabel, extraAria, children, disabled, extraClasses, onClick, }: ButtonProps) => React.JSX.Element;
export default Button;
