import React from 'react';
import { LabelProps } from './Label.types';
declare const Label: ({ children, htmlFor, error, extraClasses, required, title }: LabelProps) => React.JSX.Element;
export default Label;
