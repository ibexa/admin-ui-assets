import React from 'react';
import { BaseInputProps } from './BaseInput.types';
export declare const BaseInput: ({ name, disabled, error, className, extraInputAttrs, id, ref, required, size, title, type, value, }: BaseInputProps) => React.JSX.Element;
