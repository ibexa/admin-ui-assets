import React from 'react';
import { ExpanderProps } from './Expander.types';
declare const Expander: ({ onClick, collapseLabel, expandLabel, hasIcon, isExpanded, type }: ExpanderProps) => React.JSX.Element;
export default Expander;
