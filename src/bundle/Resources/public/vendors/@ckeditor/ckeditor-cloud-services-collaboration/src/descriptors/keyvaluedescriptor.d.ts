import Descriptor, { IDescriptorBody } from './../descriptor.js';
export default class KeyValueDescriptor extends Descriptor {
    static readonly DESCRIPTOR_NAME: string;
    static readonly DESCRIPTOR: IDescriptorBody;
}
