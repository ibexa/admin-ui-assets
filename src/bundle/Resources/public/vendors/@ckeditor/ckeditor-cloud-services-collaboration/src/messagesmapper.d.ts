/**
 * Mapper class used to convert messages types to readable names for metrics purposes.
 */
export default class MessagesMapper {
    /**
     * Maps message type (number) to string name.
     */
    static typeToStringName(messageType: number): string | number;
}
